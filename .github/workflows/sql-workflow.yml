# .github/workflows/sql-workflow.yml
on:
 workflow_dispatch:
 push:
   branches:
     - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Script
      run: ./script.ps1
      shell: pwsh
